server.port: 8081

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/animal-app?serverTimezone=UTC

  jpa:
    properties.hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
    jpa.hibernate.ddl-auto: update
    show-sql: true
